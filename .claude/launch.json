{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "watchlist",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["-c", "[ -d node_modules ] || npm ci --prefer-offline --no-audit --no-fund; npm run dev"],
      "port": 3000,
      "autoPort": true
    },
    {
      "name": "watchlist-demo",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["-c", "[ -d node_modules ] || npm ci --prefer-offline --no-audit --no-fund; NEXT_PUBLIC_DEMO_MODE=1 npx next dev --port 3002"],
      "port": 3002,
      "autoPort": true
    }
  ]
}
